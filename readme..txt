This code was used on a presentation/live demo to demonstrate several behaviors of the GC. Such as what happens if the finalizer thread is blocked by a uncorrectly released COM object.

The PPT isn't a presentation. It contains notes used by the presentator.